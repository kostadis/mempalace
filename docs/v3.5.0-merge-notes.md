# v3.5.0 merge — baseline & pre-flight notes

Sub-issue A of #23. Baseline for merging upstream `v3.5.0` into `kostadis-dev` (currently v3.3.5 + 67 private commits) via `merge/v3.5.0-into-kostadis-dev`.

## Scale of the delta

`v3.3.5..v3.5.0` = **525 commits**, 303 files, **+60,895 / −1,787 lines**. This is an order of magnitude larger than the v3.3.4→v3.3.5 merge (41 commits) that `divergence.md` describes.

| Release | Commits | version.py | Notes |
|---|---:|---|---|
| v3.3.5 → **v3.3.6** | 227 | 3.3.6 | *Not a patch.* Big feature release: extract-mode, hallways/tunnels, living-memory dynamics, embeddinggemma default, `wing_api` routing. |
| v3.3.6 → **v3.4.0** | 92 | 3.4.0 | **Pluggable backend framework lands here** (registry, pgvector, sidecar — RFC 001). No CHANGELOG stanza exists for 3.4.0. |
| v3.4.0 → **v3.4.1** | 84 | 3.4.1 | Cursor plugin + `hooks/cursor/`, Antigravity IDE support, embeddinggemma OOM fix. |
| v3.4.1 → **v3.5.0** | 122 | 3.5.0 | Local write daemon, HTTP MCP transport, `mempalace_checkpoint` / `mempalace_delete_by_source` / `source_file` filter, backend pagination. |

## Divergence radar — the original 7 sites (from `divergence.md`)

Commits in `v3.3.5..v3.5.0` touching each site's primary file:

| § | Site | File | Touching commits |
|---|---|---|---:|
| 1 | Closet/primary split | `mempalace/searcher.py` | 14 |
| 2 | Parallel mine pipeline | `mempalace/miner.py` | 35 |
| 3 | Palace addressability | `mempalace/mcp_server.py` | **82** |
| 4 | `mine_palace_lock` reentrance | miner/lock code | (e.g. #1477 `mine_convos` under lock) |
| 5 | Hooks architecture | `mempalace/hooks_cli.py` + `hooks/` | 18 + 22 |
| 6 | Ignore source | miner ignore paths | low |
| 7 | KG lazy cache | `mempalace/mcp_server.py` / KG | #1383 realpath+normcase cache key |

§3 (`mcp_server.py`, 82 commits) is now a **major** effort, not the modest thing `divergence.md` describes — upstream added a write daemon, HTTP transport, and three new MCP tools that all have to thread through the local per-palace cache design.

## NEW divergence sites discovered (not in `divergence.md`)

### §8 — Pluggable backend layer collision  ⚠️ HARD CONFLICT · DECISION REQUIRED

Both branches independently built a pluggable backend layer on top of the shared v3.3.5 `backends/{base,chroma}.py`, and they are **incompatible**:

- **Upstream v3.5.0** `backends/`: `base.py`, `chroma.py`, `registry.py`, `pgvector.py`, `qdrant.py`, `sqlite_exact.py`, `embedding_wrapper.py`, `_sidecar.py`, `__init__.py`. Framework introduced in **v3.4.0** (`ed77125` sidecar/qdrant identity, `ec5d1eb` pgvector + registry — RFC 001, #1730).
- **Local**: `base.py`, `chroma.py`, `registry.py`, **`turbovec.py`**, `__init__.py` (turbovec adapter + `MEMPALACE_BACKEND` routing).

Overlapping filenames → **add/add** conflict on `registry.py`, **modify/modify** on `base.py` / `chroma.py` / `__init__.py`. `git diff v3.5.0 HEAD` on these three files shows local **−1,299 lines** (local slimmed `chroma.py` by ~1,078 and `base.py` by ~316 vs upstream v3.5.0).

**The standing "keep local design" rule is WRONG here.** Keeping local `base.py`/`registry.py` would (a) discard upstream's pgvector/qdrant/sqlite_exact backends and (b) break them, since they depend on upstream's evolved base interface.

**DECISION (user, at the sub-issue A gate): adopt upstream's backend framework and re-home `turbovec` + `MEMPALACE_BACKEND` routing onto it.** The upstream pluggable-backend work (RFC 001) was done with the user's input, so convergence on it is the intent — not preservation of the local turbovec-centric `backends/` layer. Concretely, during the merges:
- Take **upstream's** `base.py`, `registry.py`, `__init__.py`, `chroma.py`, and the new `pgvector.py` / `qdrant.py` / `sqlite_exact.py` / `embedding_wrapper.py` / `_sidecar.py`.
- Rewrite local **`turbovec.py`** as an adapter conforming to upstream's evolved `BaseBackend`/`BaseCollection` interface, and register it through upstream's `registry.register(...)` rather than the local registry shape.
- Preserve the local `MEMPALACE_BACKEND` selection behavior only insofar as upstream's `resolve_backend_for_palace` / `get_backend` don't already cover it; prefer upstream's resolution path where they overlap.
- This exception to "keep local design" applies to `backends/` **only**; the original 7 divergence sites still keep local design.

This resolution shapes conflict handling from **B (v3.3.6)** onward (lean upstream on `base.py`/`chroma.py`) and is fully realized at **C (v3.4.0)** where the framework lands.

### §9 — Cursor integration duplication  (semantic dup, NOT a hard git conflict)

Both branches independently added Cursor support, on **different paths** (so they coexist rather than git-conflict):

- **Upstream v3.4.1**: `hooks/cursor/*` (7 files, bash + `lib/common.sh`) + `.cursor-plugin/`.
- **Local**: `hooks/mempal_cursor_*.sh` (4 scripts) + `cursor` harness inside `mempalace/hooks_cli.py`.

No merge conflict, but a **design-philosophy clash** (upstream bash hooks vs local Python `hooks_cli` harness) and duplicated surface. Decide post-merge whether to keep both, retire local in favor of upstream, or fold upstream's into the Python harness. Does not block the merge; flag for sub-issue F.

## Baseline test state (current tip, pre-merge)

Environment note: the shell exports `MEMPALACE_BACKEND=turbovec` (the user's chat-palace default), but the project `.venv` used by `uv run` does not register `turbovec`, so an unneutralized run aborts collection with 29 `unknown backend 'turbovec'` errors. This is fork issue #21 (env-var leakage), **not** a real baseline. Baseline is captured with `env -u MEMPALACE_BACKEND`.

Baseline command: `env -u MEMPALACE_BACKEND uv run pytest tests/ -q --ignore=tests/benchmarks`

**Result on current tip (pre-merge): 2013 passed, 15 skipped, 2 failed** (158s).

The 2 pre-existing failures (no merge performed — these are the current-tip baseline, related to fork issue #8):
- `tests/test_hook_chat_palace.py::test_every_mempalace_mine_call_targets_chat_palace`
- `tests/test_sync.py::TestSyncPalace::test_metadata_cache_cleared_on_exception` — this is the test `divergence.md` §3 marks as *deselected* (it patches the removed `_metadata_cache` global). The deselection is not taking effect in this environment, so it surfaces as a failure rather than a deselect. Worth reconciling the pytest deselect config during the merge.

Note: fork issue #8 records "6 tests fail"; only 2 fail here (backend neutralized) — #8 may be stale or count backend-dependent errors separately. Any merge sub-issue must treat **these 2** as the baseline and flag only *new* failures beyond them. Documented skips (15) per `divergence.md` "Test impact".

## Revised risk assessment

- This is a **much larger and riskier** merge than v3.3.5. The per-release decomposition (B–E) still holds, but B (v3.3.6, 227 commits) is **not** small, and C (v3.4.0) carries the backend-framework architecture decision.
- **Blocking decision before C:** how to reconcile §8 (backend layer). Options: (a) adopt upstream framework + re-home turbovec, (b) keep local + forgo pgvector/qdrant/sqlite_exact, (c) hybrid. This is a human checkpoint, not a Sonnet auto-merge.
- Sub-issue #25/B should drop the "likely small (patch release)" wording.

---

## Sub-issue B — merge v3.3.6 (COMPLETE)

`git merge v3.3.6` → 18 conflict files (+ ~160 auto-merged). Final: **2538 passed, 33 skipped, 2 pre-existing failures** (the sub-issue-A baseline: `test_hook_chat_palace::test_every_mempalace_mine_call_targets_chat_palace` and `test_sync::test_metadata_cache_cleared_on_exception`). No new failures. ruff-clean on all merge-touched files; no conflict markers.

### Conflict resolutions (keep local design + port upstream mechanics)
- Structural (`.gitignore`, `pyproject.toml`, `hooks/*.sh`, `cli.py`) — keep-both / local chat-palace pinning + upstream silent-mode.
- §1 `searcher.py` — kept two-list `primary`/`themes`; added upstream virtual-line-numbering.
- §2 `miner.py` / `convo_miner.py` — kept the local parallel pipeline (`_prepare_file`/`_embed_prepared`/`_write_prepared`); ported upstream's chunk-cap (#1455), config chunking, Tier 6a content-date/line-numbers (#1584), symlink logging, `skip_reason` 3-tuples. convo `CHUNK_SIZE` 2000→800 (nomic-2048 sanity), extract-mode isolation, `wing_api` routing.
- §3/§7 `mcp_server.py` — kept local per-palace `_kg_cache`/`_get_kg(palace_path)` + `_invalidate_metadata_cache()`; adopted #1539 chunked-drawer routed through local invalidation.
- §5 `hooks_cli.py` — adopted #1410 wing-detection, kept local Cursor branch.
- §embedding `config.py`/`embedding.py` — **unified** (user decision): `embedding_provider` top switch + `embedding_model` selects minilm/embeddinggemma within onnx; remote Ollama/OpenAI-compat kept. `embedding_model` is case-insensitive (`.lower()`).
- `palace.py` — local backend routing + upstream exceptions/`_open_collection_or_explain`.

### Silent auto-merge defect found + fixed (would have shipped broken)
`backends/chroma.py` — git auto-merged (no conflict raised) local's HNSW-quarantine divergence with upstream's #1452/#1461 changes and **dropped the `_HNSW_MISSING_METADATA_DATA_FLOOR = 1024` definition while keeping its usages** → `NameError` breaking 15 backend/HNSW tests. Restored the constant (upstream's value/placement); upstream's floor logic subsumes local's "don't-quarantine-unflushed" intent. Caught only by running the full suite — a reminder to not trust a clean auto-merge.

### Deferred (documented, not silent)
- **#1383** KG realpath/normcase cache canonicalization — kept local `_kg_cache` (abspath+expanduser) keying per §7 "keep local structure"; revisit if symlinked/case-variant palace paths matter.
- `prefetch_mined_set` convo bulk optimization — perf-only; per-file `file_already_mined(extract_mode=)` already gives correctness.

### Test-file notes
- Skipped (divergence, per §Test impact): `test_miner::test_mine_summary_separates_chunk_cap_skips` (patches monolithic `process_file`); `test_mcp_server` KG-internals/palace-less-import tests asserting upstream-only `_kg_by_path`/`_canonicalize_kg_path`.
- `test_hooks_cli.py`: the merged source uses upstream's module-level `from mempalace.config import MempalaceConfig`, so config mocks must patch `mempalace.hooks_cli.MempalaceConfig` (where it's looked up). Fixed the verbose-cursor test's own patch stack; left the shared `_capture_hook_output` helper patching `mempalace.config` so tests that wrap it with their own `hooks_cli` patch (e.g. `test_stop_hook_disabled_by_config`) stay in control.
